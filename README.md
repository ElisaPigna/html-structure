#READ ME

A structured web page demonstrating the use of:

- a header (`header`)
- navigation (`nav`)
- main area (`main`) containing nested
  - sections (`section`)
  - articles (`article`)
  - asides (`aside`)
- a footer (`footer`)
